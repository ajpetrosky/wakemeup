# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Wake Me Up' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Wake Me Up
  pod 'Alamofire', '~> 4.4'

  target 'Wake Me UpTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Wake Me UpUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
